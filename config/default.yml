#urls:
#  - "http://localhost*" #this will match URLs on localhost, this is the dev/testing default
#  - "http://www.lnwdr.de*"

server:
  port: 80
  instance_name: null

cache:
  base_path: "cache"

process:
  limit: 5

accepted_mime_types:
  - "image/jpeg"
  - "image/pjpeg"
  - "image/png"
  - "image/gif"

analysis:
  aoi_name: "AOI"

converter:
  padding_color: "#FEFEFE"
  default_mime_type: "image/jpeg"
