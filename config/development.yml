server:
  port: 8001

download:
  url_whitelist:
    - "http://localhost:8002/*"

test_server:
  enabled: true
  port: 8002

authentication:
  username: "testuser"
  password: "testpass"

logging:
  error: "stderr"
  debug: "stderr"
  access: "stdout"
