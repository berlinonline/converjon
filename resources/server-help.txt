{{name}} - {{version}}

Starts the Converjon image server.

OPTIONS:

    --help          Display this help.

    --config        Path to a configuration file.
                    Can be set multiple times to load more than one file.

    --dev           Development mode. Starts converjon with the example
                    development.yml config merged over the default.yml.
