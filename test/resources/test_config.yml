urls:
  - "http://localhost*" #this will match URLs on localhost, this is the dev/testing default

server:
  port: 8000

download:
  url_whitelist:
    - "http://localhost:10000/*"

cache:
  base_path: "/tmp/converjon/test"

authentication:
  username: "testuser"
  password: "testpass"
